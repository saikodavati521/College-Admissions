[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "admissions-model-config"
version = "1.0.0"
requires-python = ">=3.9"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["config"]
